// Environnement de production (Railway)
// TODO: remplacer par l'URL réelle du backend déployé sur Railway.
export const environment = {
  production: true,
  apiUrl: 'https://API_URL_RAILWAY/api',
};
